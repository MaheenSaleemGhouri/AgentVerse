"""Application layer: job-handling use cases, depending inward on
`domain` only (CLAUDE.md §5). Empty in Phase 0 — the first use case
(consuming a job off the Redis-backed queue) lands in Phase 3.
"""
